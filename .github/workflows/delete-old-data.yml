name: Delete old data

on:
  schedule:
    - cron:  '*/10 * * * *'

  workflow_dispatch:

jobs:
  delete-data:
    runs-on: ubuntu-latest
    steps:
      - name: Delete old data
        run: cf8 run-task sea-beta-loadtest --command "echo 1" --name delete-old-data-task
        
