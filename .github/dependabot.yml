version: 2
updates:
  - package-ecosystem: "nuget"
    directory: "/"
    schedule:
      interval: "weekly"
    # we may not be able to respond to pull requests immediately
    # prioritise showing them all
    open-pull-requests-limit: 100
    # group together minor & patch updates. major ones get a PR per change
    groups:
      non-major:
        update-types:
          - "minor"
          - "patch"
    ignore:
      # ensure FluentAssertions doesn't update to 8.x as its new license prohibit commercial use
      - dependency-name: "FluentAssertions"
        versions: ["8.*"]

  - package-ecosystem: "npm"
    directory: "SeaPublicWebsite"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 100
    # group all npm updates
    groups:
      npm:
        update-types:
          - "major"
          - "minor"
          - "patch"
    ignore:
      # ensure govuk-frontend doesn't update to 5.x until we're ready
      - dependency-name: "govuk-frontend"
        versions: [">=4.x"]
