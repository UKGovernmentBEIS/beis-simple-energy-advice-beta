@model SeaPublicWebsite.Models.EnergyEfficiency.DownloadPdfButtonViewModel

@using (Html.BeginForm("GenerateActionPlanPdf_Post", "EnergyEfficiency", new { reference = Model.Reference }))
{
    <button type="submit" class="govuk-link sea-download-button @Model.ButtonClass">
        <img src="/assets/images/download-icon.png" aria-hidden="true" />
        <span>Download Action Plan PDF</span>
    </button>
}
